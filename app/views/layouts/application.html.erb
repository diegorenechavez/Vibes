<!DOCTYPE html>
<html>
  <head>
    <title>Vibes</title>
    <link rel="icon" type="image/x-icon" href="<%= image_url('circle-cropped.png') %>" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_include_tag 'application' %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <script type="text/javascript">
  window.logoURL = "<%= image_url('transparentLogo.png') %>";
  window.faviconURL = "<%= image_url('circle-cropped.png') %>";
</script>
<%if current_user %>
  <script>
  window.currentUser = {
    "id": <%= current_user.id%>,
    "username": "<%= current_user.username%>",
    "email": "<%= current_user.email%>"
  };
  </script>
<%end%>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
